# Leave unset for local development -- Vite proxies /api to localhost:4000.
# In production point this at the deployed API, with no trailing slash:
# VITE_API_URL=https://triage-desk-api.onrender.com
VITE_API_URL=
